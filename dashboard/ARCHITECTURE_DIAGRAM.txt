```
╔══════════════════════════════════════════════════════════════════════════════╗
║                     NEURAL COMMAND CENTER DASHBOARD                          ║
║                      Production-Grade Architecture                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                              USER BROWSER                                     │
│                         http://localhost:3000                                 │
└────────────────────────────────┬─────────────────────────────────────────────┘
                                 │
                    ┌────────────┴───────────┐
                    │    Nginx Reverse       │
                    │    Proxy (Alpine)      │
                    │    Port 3000           │
                    └────────────┬───────────┘
                                 │
              ┌──────────────────┼──────────────────┐
              │                  │                  │
       ┌──────▼──────┐    ┌─────▼─────┐   ┌───────▼────────┐
       │  /          │    │ /api/*    │   │ /ws/*          │
       │  Static     │    │ Proxy     │   │ WebSocket      │
       │  Files      │    │ Pass      │   │ Upgrade        │
       └──────┬──────┘    └─────┬─────┘   └───────┬────────┘
              │                 │                  │
       ┌──────▼──────────────────▼──────────────────▼────────┐
       │              FastAPI Backend                         │
       │              Python 3.11 + Uvicorn                   │
       │              Port 8000 (internal)                    │
       │                                                      │
       │  ┌─────────────────────────────────────────────┐   │
       │  │         REST API (Async/Await)              │   │
       │  │                                             │   │
       │  │  GET  /                    Health check    │   │
       │  │  GET  /api/status          System status   │   │
       │  │  POST /api/cognition/start  Start engine   │   │
       │  │  POST /api/cognition/stop   Stop engine    │   │
       │  │  GET  /api/cognition/snapshot  State       │   │
       │  │  POST /api/cognition/interact  Commands    │   │
       │  │                                             │   │
       │  │  GET/POST/PUT/DELETE /api/governance/*     │   │
       │  │  GET/POST /api/training/metrics            │   │
       │  │  GET/POST /api/edge/devices                │   │
       │  └─────────────────────────────────────────────┘   │
       │                                                      │
       │  ┌─────────────────────────────────────────────┐   │
       │  │      WebSocket Server (10 Hz Updates)       │   │
       │  │                                             │   │
       │  │  WS /ws/cognition   Real-time cognition    │   │
       │  │  WS /ws/training    Metrics streaming      │   │
       │  └─────────────────────────────────────────────┘   │
       │                                                      │
       │  ┌─────────────────────────────────────────────┐   │
       │  │      Background Async Task Loop             │   │
       │  │                                             │   │
       │  │  while cognition_running:                   │   │
       │  │    state = demo.get_state()                │   │
       │  │    await broadcast(state)                  │   │
       │  │    await asyncio.sleep(0.1)  # 10 Hz       │   │
       │  └─────────────────────────────────────────────┘   │
       │                                                      │
       │  ┌─────────────────────────────────────────────┐   │
       │  │         Cognition Engine                    │   │
       │  │         (demo_cognition.py)                 │   │
       │  │                                             │   │
       │  │  CognitionDemo(field_size=100)             │   │
       │  │  - process_input()                         │   │
       │  │  - update_field()                          │   │
       │  │  - compute_attention()                     │   │
       │  │  - detect_conflicts()                      │   │
       │  └─────────────────────────────────────────────┘   │
       └──────────────────────────────────────────────────────┘
                                 │
                    ┌────────────▼───────────┐
                    │  DashboardState        │
                    │  (In-Memory)           │
                    │                        │
                    │  - cognition_running   │
                    │  - training_metrics[]  │
                    │  - policies[]          │
                    │  - edge_devices{}      │
                    └────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                          REACT FRONTEND (SPA)                                 │
│                     Vite 5.0.4 + React 18.2.0                                 │
└──────────────────────────────────────────────────────────────────────────────┘

                    ┌────────────────────────┐
                    │      App.jsx           │
                    │   (React Router)       │
                    └────────┬───────────────┘
                             │
              ┌──────────────┼──────────────────┬───────────────┐
              │              │                  │               │
       ┌──────▼──────┐ ┌────▼─────┐ ┌─────────▼────┐ ┌────────▼─────┐
       │ Cognition   │ │ Training │ │ Governance   │ │ Edge         │
       │ View        │ │ View     │ │ View         │ │ View         │
       └──────┬──────┘ └────┬─────┘ └─────────┬────┘ └────────┬─────┘
              │             │                  │               │
              │             │                  │               │
       ┌──────▼─────────────────────────────────────────────────────────┐
       │              Zustand Store (dashboardStore.js)                 │
       │                                                                 │
       │  State:                           Actions:                     │
       │  - cognitionData                  - startCognition()           │
       │  - isRunning                      - stopCognition()            │
       │  - wsConnected                    - sendInteraction()          │
       │  - trainingMetrics                - createPolicy()             │
       │  - policies                       - updateDevice()             │
       │  - edgeDevices                                                 │
       │                                                                 │
       │  WebSocket:                       HTTP:                        │
       │  - ws://localhost:8000/ws/cognition  - axios REST client       │
       │  - Auto-reconnect (3s delay)         - GET/POST/PUT/DELETE     │
       │  - onmessage → update state                                    │
       └─────────────────────────────────────────────────────────────────┘
                                     │
              ┌──────────────────────┼──────────────────────┐
              │                      │                      │
       ┌──────▼──────┐    ┌──────────▼──────────┐   ┌──────▼──────┐
       │ Cognitive   │    │ Intent &            │   │ World       │
       │ Physiology  │    │ Deliberation        │   │ Interface   │
       │ Plane       │    │ Plane               │   │ Plane       │
       └──────┬──────┘    └──────────┬──────────┘   └──────┬──────┘
              │                      │                      │
       ┌──────▼──────┐    ┌──────────▼──────────┐   ┌──────▼──────┐
       │ Canvas      │    │ Canvas              │   │ React       │
       │ Rendering   │    │ Rendering           │   │ Components  │
       │             │    │                     │   │             │
       │ 4 Layers:   │    │ Goal Nodes:         │   │ Sensors:    │
       │ - Perception│    │ - Circles           │   │ - Visual    │
       │ - Integration│   │ - Hypotheses        │   │ - Temporal  │
       │ - Reasoning │    │ - Orbits            │   │ - Pattern   │
       │ - Action    │    │                     │   │             │
       │             │    │ Interactions:       │   │ Actions:    │
       │ Visuals:    │    │ - Click to pin      │   │ - Approve ✓ │
       │ - Pulses    │    │ - Hover for info    │   │ - Reject ✗  │
       │ - Flows     │    │                     │   │             │
       │ - Heat maps │    │                     │   │             │
       │ - Conflicts │    │                     │   │             │
       │             │    │                     │   │             │
       │ 60 FPS      │    │ 60 FPS              │   │ Real-time   │
       └─────────────┘    └─────────────────────┘   └─────────────┘

                    ┌────────────────────────┐
                    │  Interaction Panel     │
                    │                        │
                    │  - Signal Injection    │
                    │  - Cognitive Probes    │
                    │  - Safety Governors    │
                    │                        │
                    │  (No Chat Box)         │
                    └────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════════╗
║                         DEPLOYMENT OPTIONS                                    ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ OPTION 1: Development Mode                                                  │
│                                                                              │
│  Terminal 1:  cd dashboard/backend && python main.py                        │
│  Terminal 2:  cd dashboard/frontend && npm run dev                          │
│                                                                              │
│  Frontend: http://localhost:5173 (Vite dev server with HMR)                 │
│  Backend:  http://localhost:8000                                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ OPTION 2: Docker Compose (Recommended)                                      │
│                                                                              │
│  ./start_dashboard.sh  OR  docker-compose up -d                             │
│                                                                              │
│  ┌────────────────┐         ┌────────────────┐                              │
│  │ Frontend       │         │ Backend        │                              │
│  │ Container      │◄────────│ Container      │                              │
│  │ (Nginx)        │         │ (Uvicorn)      │                              │
│  │ Port: 3000     │         │ Port: 8000     │                              │
│  └────────────────┘         └────────────────┘                              │
│          │                          │                                        │
│          └──────────┬───────────────┘                                        │
│                     │                                                        │
│              ┌──────▼──────┐                                                 │
│              │ quadra-     │                                                 │
│              │ network     │                                                 │
│              │ (bridge)    │                                                 │
│              └─────────────┘                                                 │
│                                                                              │
│  Frontend: http://localhost:3000                                            │
│  Backend:  http://localhost:8000                                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ OPTION 3: Kubernetes (Future)                                               │
│                                                                              │
│  kubectl apply -f k8s/                                                      │
│                                                                              │
│  ┌────────────┐       ┌────────────┐       ┌────────────┐                  │
│  │ Ingress    │──────►│ Frontend   │       │ Backend    │                  │
│  │ Controller │       │ Service    │       │ Service    │                  │
│  └────────────┘       └─────┬──────┘       └─────┬──────┘                  │
│                             │                    │                          │
│                      ┌──────▼──────┐      ┌──────▼──────┐                  │
│                      │ Frontend    │      │ Backend     │                  │
│                      │ Pods × 3    │      │ Pods × 3    │                  │
│                      └─────────────┘      └─────┬───────┘                  │
│                                                  │                          │
│                                           ┌──────▼──────┐                  │
│                                           │ Redis       │                  │
│                                           │ Cluster     │                  │
│                                           └─────────────┘                  │
│                                                                              │
│  Horizontal Pod Autoscaling: 3-10 replicas                                  │
│  Load Balancer: Nginx Ingress                                               │
│  Persistent Storage: StatefulSet for cognition state                        │
└─────────────────────────────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════════╗
║                          DATA FLOW DIAGRAM                                    ║
╚══════════════════════════════════════════════════════════════════════════════╝

USER ACTION: Click "Start Cognition"
     │
     ▼
React Component (CognitionView)
     │
     │ onClick → startCognition()
     ▼
Zustand Store
     │
     │ axios.post('/api/cognition/start')
     ▼
FastAPI Endpoint
     │
     │ DashboardState.cognition_running = True
     ▼
Background AsyncIO Task
     │
     │ while cognition_running:
     │   state = demo.get_state()
     │   await manager.broadcast(state)
     ▼
WebSocket Manager
     │
     │ for connection in active_connections:
     │   await connection.send_json(state)
     ▼
Browser WebSocket Client
     │
     │ ws.onmessage(event)
     ▼
Zustand Store Update
     │
     │ set({ cognitionData: data })
     ▼
React Re-render (Subscribed Components)
     │
     ├─► CognitivePhysiologyPlane → Canvas update (60fps)
     ├─► IntentDeliberationPlane → Canvas update (60fps)
     └─► WorldInterfacePlane → DOM update
     
     
╔══════════════════════════════════════════════════════════════════════════════╗
║                      INTEGRATION POINTS                                       ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ EXISTING QUADRA COMPONENTS → DASHBOARD                                      │
│                                                                              │
│  train_live_dashboard.py                                                    │
│       │                                                                      │
│       │ requests.post('/api/training/metrics', {...})                       │
│       ▼                                                                      │
│  FastAPI Backend                                                            │
│       │                                                                      │
│       │ DashboardState.training_metrics.append(metric)                      │
│       ▼                                                                      │
│  WebSocket Broadcast                                                        │
│       │                                                                      │
│       ▼                                                                      │
│  React TrainingView (Recharts)                                              │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  model_monitoring.py (Edge Device)                                          │
│       │                                                                      │
│       │ requests.post('/api/edge/devices', {...})                           │
│       ▼                                                                      │
│  FastAPI Backend                                                            │
│       │                                                                      │
│       │ DashboardState.edge_devices[id] = device                            │
│       ▼                                                                      │
│  React EdgeView (Device List)                                               │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════════╗
║                        FILE ORGANIZATION                                      ║
╚══════════════════════════════════════════════════════════════════════════════╝

cognitionsim-a.i./
│
├── dashboard/
│   ├── README.md                    📖 Setup guide
│   ├── API.md                       📖 API reference
│   ├── NEURAL_COMMAND_CENTER.md     📖 Architecture
│   ├── DASHBOARD_COMPLETE.md        📖 Summary
│   ├── docker-compose.yml           🐳 Orchestration
│   │
│   ├── backend/
│   │   ├── main.py                  🐍 FastAPI app (650 lines)
│   │   ├── requirements.txt         📦 Dependencies
│   │   ├── Dockerfile               🐳 Container
│   │   └── .env.example             ⚙️  Config
│   │
│   └── frontend/
│       ├── package.json             📦 Dependencies
│       ├── vite.config.js           ⚙️  Build
│       ├── Dockerfile               🐳 Multi-stage
│       ├── nginx.conf               🔧 Proxy
│       ├── .env.example             ⚙️  Config
│       │
│       ├── src/
│       │   ├── App.jsx              ⚛️  Main app
│       │   ├── main.jsx             ⚛️  Entry
│       │   │
│       │   ├── components/
│       │   │   ├── CognitivePhysiologyPlane.jsx
│       │   │   ├── IntentDeliberationPlane.jsx
│       │   │   ├── WorldInterfacePlane.jsx
│       │   │   └── InteractionPanel.jsx
│       │   │
│       │   ├── views/
│       │   │   ├── CognitionView.jsx
│       │   │   ├── TrainingView.jsx
│       │   │   ├── GovernanceView.jsx
│       │   │   └── EdgeView.jsx
│       │   │
│       │   └── store/
│       │       └── dashboardStore.js
│       │
│       └── public/
│
├── start_dashboard.sh              🚀 Docker launch
├── start_dashboard_dev.sh          🚀 Dev mode
└── DASHBOARD_SUMMARY.md            📖 Overview


════════════════════════════════════════════════════════════════════════════════
                           READY FOR DEPLOYMENT ✅
════════════════════════════════════════════════════════════════════════════════
```
